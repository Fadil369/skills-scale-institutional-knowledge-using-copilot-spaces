{
  "name": "oracle-claim-scanner",
  "version": "1.0.0",
  "private": true,
  "description": "Oracle ERP claim scanner with Puppeteer Browser Rendering",
  "scripts": {
    "dev": "wrangler dev --port 8791",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail oracle-claim-scanner",
    "secrets:set": "bash ../../scripts/set-oracle-secrets.sh"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^0.0.7"
  },
  "devDependencies": {
    "wrangler": "^3.40.0"
  }
}
